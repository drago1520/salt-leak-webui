{
  "name": "@repo/mqtt-broker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun --preload ./instrument.ts index.ts",
    "dev": "bun --watch --preload ./instrument.ts index.ts"
  },
  "devDependencies": {
    "@repo/ts-config": "workspace:*",
    "@types/bun": "^1.3.13"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/shared": "workspace:*",
    "@sentry/bun": "^10.50.0",
    "aedes": "^1.0.2",
    "dotenv": "^17.4.2"
  }
}
